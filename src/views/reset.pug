<!doctype html>
html(lang="en")
  head
    include partials/head
  body.login
    .contenedor-formulario
      h1= titlePage 
      p Add your email for reset your password
      if locals.messages
        .alertas
          each category in Object.keys(locals.messages)
            each error in messages[category]
              .alerta(class=`${category}`)
                p= error
      form.caja-login(method="POST" action="/auth/reset")
        .campo
          label(for="email") E-mail:
          input(type="email" name="email" placeholder="Email" value= email)
        .campo.enviar
          input.boton(type="submit" value="Reset Password")
        .campo.acciones
          a(href='/auth/login') Log in

